# Upstream Catch-Up Merge — 2026-07-07

Merge of `upstream/npcbots_3.3.5` @ `665021b72` (trickerer/AzerothCore-wotlk-with-NPCBots) into `araxia-main`, executed on branch `araxia-merge-20260707`. Merge commit: `87f6e9f30`. 4,003 upstream commits since merge-base `f9e5113e1` (2025-01-10). Planning docs: `docs/stories/`.

## Manual conflict resolutions

| File | Resolution |
|---|---|
| `src/server/game/AI/NpcBots/botmgr.h/cpp` | Took upstream (config system moved out of `BotMgr`). Araxia `NpcBot.MaxLevel` re-implemented in the new `BotCfg` class: `botconfig.h/cpp` (`_maxBotLevel` static + `BotCfg::GetMaxBotLevel()`, loaded in `_loadConfig`). |
| `src/server/game/AI/NpcBots/bot_ai.cpp` | Took upstream, re-applied: both `SetStats` level caps now use `BotCfg::GetMaxBotLevel()` (upstream: `DEFAULT_MAX_LEVEL` / `DEFAULT_MAX_LEVEL + 3`); `_listAuras` returns `std::string` with `bool sendChat = true` param (used by `BotAIEluna::BotDump`). |
| `src/server/game/AI/NpcBots/bot_ai.h` | Took upstream, re-applied: `#include "bot_ai_eluna.h"`, `class BotAIEluna;` forward decl, `friend class BotAIEluna;`, `_listAuras` signature. |
| `apps/docker/Dockerfile` | Upstream Ubuntu 24.04 base + 24.04 package lists (`default-libmysqlclient-dev`, `libicu74`, `liblzma-dev`, `adduser`); kept araxia `ARG TARGETARCH`/`TZ` (Mac support) and the `DEBUGGER` lldb/gdb install block. |
| `.vscode/settings.json` | Union of both sides (cSpell + C_Cpp/cmake settings, build dir `var/build`). |
| `apps/startup-scripts/gdb.conf` | Restored araxia copy — upstream deleted it in the startup-scripts restructure, but our Dockerfile `COPY`s it. |

Auto-merged but verified: `modules/CMakeLists.txt` kept the araxia `${CMAKE_SOURCE_DIR}/src/common/Logging` module include alongside upstream's new mod-ale support.

Follow-up commits: `8dde2b839` documents `NpcBot.MaxLevel` in `worldserver.conf.dist` (new; key previously existed only in code).

## Validation

- Full `worldserver` + `authserver` build: **passed** (clang 18.1.3, Ubuntu 24.04 container, `-DSCRIPTS=static -DMODULES=static`).
- Binary smoke: worldserver boots, parses config (incl. `NpcBot.MaxLevel`), initializes C++ scripts, fails only at MySQL connect as expected with no DB.
- Fresh-DB creation + full SQL update chain: **passed** — worldserver auto-setup against a scratch MySQL 8.4 created `acore_auth`/`acore_characters`/`acore_world` from base files and applied every update (world DB: 1,287 new + 1,384 archived); server then proceeded to world loading and stopped only at missing client map files, as expected without game data.
- Release docker image: **built** (`acore/ac-wotlk-worldserver:release-amd64` via `docker-compose.release.yml`, Ubuntu 24.04 base with merged Dockerfile).
- **Production snapshot migration: passed** (2026-07-07, snapshot `araxia_2026-07-07-081701`). Restored into a scratch MySQL 8.4 container; merged worldserver auto-updater applied auth 3 new / characters 5 new / world **821 new** updates (1,340 queries), ACDB 335.13-dev → 335.17-dev, **zero SQL errors**. Data integrity verified: characters (610), accounts (286), hired npcbots (314), item_instance (65,083), `custom_transmogrification` (73), mythic+ tables — all unchanged. Server proceeded past DB stage and stopped only at missing client map files, as expected.
- In-game smoke test: **not yet run** (needs client data + a live session).

## Config changes for ops (merge-base → merged)

Deployed `worldserver.conf` needs reconciliation. Full key diff:

**NpcBots — renamed/replaced keys (action required if set):**
- `NpcBot.XpReduction.Amount`, `NpcBot.XpReduction.StartingNumber`, `NpcBot.XpReduction.Blizzlike.Enable`, `NpcBot.XpReduction.Blizzlike.GroupOnly` → **removed**; replaced by `NpcBot.XpReduction.Enable`, `NpcBot.XpReduction.GroupOnly`, `NpcBot.XpReduction.Extra.Amount`, `NpcBot.XpReduction.Extra.StartingNumber`
- `NpcBot.MaxLevel` — araxia key, now documented in conf.dist (default 80)

**NpcBots — new keys (defaults apply if absent):** `NpcBot.DungeonBots.Enable`, `NpcBot.DungeonBots.MaxItemLevel.Ratio`, `NpcBot.HonorReduction.Enable`, `NpcBot.HonorReduction.GroupOnly`, `NpcBot.SharedOwnership.Options`, `NpcBot.SharedOwnership.MaxOwners`, `NpcBot.WanderingBots.FreeLoot.Skinning`

**Core — removed keys (delete from deployed conf if present):** `AllowTwoSide.AddFriend`, `AllowTwoSide.Interaction.Emote`, `AllowTwoSide.Interaction.Mail`, `AllowTwoSide.Trade`, `AllowTwoSide.WhoList`, `Arena.ArenaSeason.ID`, `Arena.ArenaSeason.InProgress`, `Metric.ConnectionInfo` (→ `Metric.InfluxDB.*`), `PacketSpoof.Policy`, `SetAllCreaturesWithWaypointMovementActive`, `TeleportTimeoutFar`, `TeleportTimeoutNear`

**Core — new keys worth reviewing (behavior-relevant defaults):** `Trial.*` (trial account restrictions), `Respawn.Dynamic*`, `ChatFilter.*`, `ChatLog.Enable`, `DungeonFinder.*`, `CreatureLeashRadius`, `Creature.RepositionAgainstNpcs`, `InfiniteAmmo.Enabled`, `Battleground.PrepTime`/`Arena.PrepTime`, `Rate.XP.BattlegroundBonus`, `Wintergrasp.*`, `ScourgeInvasion.*`, `Sunsreach.CounterMax`, `Network.UseSocketActivation` (also authserver)

## Outstanding (Story 2)

- [x] DB update run against a **production snapshot** — passed, see Validation
- [ ] In-game smoke: bot hire, `NpcBot.MaxLevel` respected, wanderers spawn, BotDump/`_listAuras`
- [ ] Reconcile deployed configs per the key diff above
- [ ] Cutover plan to `araxia-main`
